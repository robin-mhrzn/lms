using System.ComponentModel;

namespace API.Order.Shared
{
    public class EnumCollection
    {
        public enum StripeStatus
        {
            succeeded,
            pending
        } 
    }
}
